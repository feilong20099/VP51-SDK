#ifndef MKCROSSAPI_INCLUDE_H_
#define MKCROSSAPI_INCLUDE_H_

#include "MKCardApiDef.h"

#ifdef __cplusplus
extern "C" {
#endif
	//////////////////////////////////////////////////////////////////////////
	// 系统资源初始化
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_SystemInit(void* pInitParam);

	//////////////////////////////////////////////////////////////////////////
	// 系统资源释放
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_SystemUninit(BOOL bProcessDetach = FALSE);

	//////////////////////////////////////////////////////////////////////////
	// 获取设备信息
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_GetDeviceinfo(
		OUT MKDeviceInfo_t* p_info, 		// 设备信息结构指针
		IN	unsigned int size);				// 设备信息结构长度

	/////////////////////////////////////////////////////////////////////////
	// 打开卡 
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_OpenCard(
		OUT HANDLE* p_card_handle,			// 卡操作句柄
		IN	const MKCardInfo_t* p_card_info);		// 卡信息结构指针

	//////////////////////////////////////////////////////////////////////////
	// 关闭卡
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_CloseCard(
		IN	HANDLE card_handle);			// 卡操作句柄

	//////////////////////////////////////////////////////////////////////////
	// 获取指定端口上的媒体信息
	// 为了兼容老版本保留接口，使用MKCrossApi_GetMediaInfo_2代替
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_GetMediaInfo(
		OUT MKMediaInfo_t* p_info,			// 媒体信息结构指针
		IN	unsigned int size,				// 媒体信息结构长度
		IN  HANDLE card_handle,				// 卡操作句柄
		IN  int port_index);				// 端口序号

	//////////////////////////////////////////////////////////////////////////
	// 获取指定端口上的媒体信息
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_GetMediaInfo_2(
		OUT MKVideoInfoEx_t* p_video_info,	// 视频信息结构指针
		OUT MKAudioInfo_t* p_audio_info,	// 音频信息结构指针
		IN  HANDLE card_handle,				// 卡操作句柄
		IN  int port_index);				// 端口序号

	//////////////////////////////////////////////////////////////////////////
	// 设置指定端口的信号改变回调函数
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_SetSignalChangeCallBack(
		IN  MKApi_CaptureStreamCb p_event_cb,	// 事件回调函数
		IN  void* p_event_user,					// 回调函数用户数据
		IN  BOOL bAdd,							// 添加或移除
		IN  HANDLE card_handle,					// 卡操作句柄
		IN  int port_index);					// 端口序号

	//////////////////////////////////////////////////////////////////////////
	// 获取控制参数
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_GetControl(
		OUT void* p_param,					// 控制参数/结构指针
		IN	unsigned int size,				// 控制参数/结构长度
		IN  MKControlType type,				// 控制类型
		IN  HANDLE card_handle,				// 卡操作句柄
		IN  int port_index);				// 端口序号

	//////////////////////////////////////////////////////////////////////////
	// 设置控制参数
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_SetControl(
		IN	void* p_param,					// 控制参数/结构指针
		IN	unsigned int size,				// 控制参数/结构长度 
		IN	MKControlType type,				// 控制类型
		IN  HANDLE card_handle,				// 卡操作句柄
		IN  int port_index);				// 端口序号

	//////////////////////////////////////////////////////////////////////////
	// 开始采集
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_BeginCapture(
		OUT HANDLE* p_capture_handle,			// 返回的采集句柄
		IN OUT MKCaptureStreamParam_t* p_param, // 采集设置结构指针
		IN unsigned int size,					// 采集设置结构长度
		IN HANDLE card_handle,					// 卡操作句柄
		IN int port_index);						// 端口序号

	//////////////////////////////////////////////////////////////////////////
	// 停止采集
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_StopCapture(
		IN HANDLE capture_handle);		    // 采集操作句柄

	//////////////////////////////////////////////////////////////////////////
	// 开始播出
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_BeginSend(
		OUT HANDLE* p_send_handle,			// 播出句柄
		IN  MKSendParam_t* p_param,			// 播出设置结构指针
		IN	unsigned int size,				// 播出设置结构长度
		IN  HANDLE card_handle,				// 卡操作句柄
		IN  int port_index);				// 端口序号

	//////////////////////////////////////////////////////////////////////////
	// 停止播出
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_StopSend(
		IN HANDLE send_handle);				// 播出操作句柄

	//////////////////////////////////////////////////////////////////////////
	// 设置播出数据
	//////////////////////////////////////////////////////////////////////////
	MKError	MKCrossApi_SetData(
		IN	MKMediaData_t* p_data,			// 播出数据结构指针,视频缓冲要多分配0x24000个Byte
		IN	unsigned int size,				// 播出数据结构长度
		IN	HANDLE send_handle);			// 播出操作句柄

	//////////////////////////////////////////////////////////////////////////
	// 获取设备名称
	//////////////////////////////////////////////////////////////////////////
	const wchar_t* MKCrossApi_GetCardDescription(MKCardType type);

	int MKCrossApi_GetCardSubDesc(
		IN const MKCardInfo_t* p_card_info,	// 卡信息结构指针
		IN OUT wchar_t* pszDesc,			// 返回描述字符串
		IN int chMaxCount);					// 字符串允许存放的最多字符个数

	//////////////////////////////////////////////////////////////////////////
	// 获取播出支持的视频制式
	//////////////////////////////////////////////////////////////////////////
	BOOL MKCrossApi_GetSendVSTDArray(
		IN const MKCardInfo_t* p_card_info,	// 卡信息结构指针
		OUT MKVideoStandard* p_vstd_array,	// 返回支持的制式数组
		IN int array_alloc_count,			// 制式数组分配的个数
		OUT int* p_support_count);			// 返回支持的制式个数

	//////////////////////////////////////////////////////////////////////////
	// 检查是否是播出支持的视频制式
	//////////////////////////////////////////////////////////////////////////
	BOOL MKCrossApi_IsSendVSTDSupport(
		IN MKVideoStandard vstd,				// 待检查的视频制式
		IN const MKCardInfo_t* p_card_info);	// 卡信息结构指针

	//////////////////////////////////////////////////////////////////////////
	// 设置设备移除新增的通知回调
	//////////////////////////////////////////////////////////////////////////
	MKError MKCrossApi_SetDeviceChangedNotify(
		MK_DeviceChangedCb pCB,					// 回调函数
		void* p_user);							// 回调函数参数

	//////////////////////////////////////////////////////////////////////////
	// private
	//////////////////////////////////////////////////////////////////////////
	void* MKCrossApi_GetLogger();
	void* MKCrossApi_GetCardConfig();
	void* MKCrossApi_GetDebugInfo();

#ifdef __cplusplus
}
#endif

#endif